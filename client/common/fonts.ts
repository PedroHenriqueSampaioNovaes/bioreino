import { Fredoka, Catamaran } from 'next/font/google';

export const fredoka = Fredoka({
  variable: '--font-primary-fredoka',
  subsets: ['latin'],
});

export const catamaran = Catamaran({
  variable: '--font-secondary-catamaran',
  subsets: ['latin'],
});
